# 고도화 로드맵 (8단계 완료 후 추가 작업)

기본 기획서(1~8단계)는 이미 완료되었고, 아래는 남은 시간(약 6일) 동안
"진짜 사람/연인과 대화하는 느낌"을 강화하고, 제출 완성도를 높이기 위한
추가 고도화 작업입니다.

**작업 순서는 A → B → C → D → H → E → F → G → I 순서를 따르되,**
**각 항목 작업 전 반드시 이 문서에서 해당 항목을 다시 확인하고 시작할 것.**
각 항목 완료 후에는 반드시 실제 브라우저 대화(또는 해당 검증 방법)로
확인하고, 완료 여부를 이 문서의 체크박스에 표시.

이미 완료된 선행 작업(참고용):
- [x] "여러분" 등 복수형 호칭 제거, 1:1 대화 명시
- [x] 질문 회피 패턴 수정 (질문에 구체적으로 먼저 답한 뒤 되묻기)
- [x] 질문/공감·리액션 마무리 비율 50:50 균형 (10턴 실측 검증 완료)

---

## 카테고리 개요

- **A~D**: 대화의 자연스러움 / 관계의 리얼함 (텍스트·UX 레벨 개선)
- **H**: 안정성/신뢰성 (배포 전 반드시 확인 — 우선순위 높음)
- **E**: RAG 자체 고도화 (알려진 한계 극복 시도)
- **F**: 관계/캐릭터 시스템 심화
- **G**: UI/UX 완성도 (있으면 좋은 것)
- **I**: 제출물 문서화 (반드시 필요, 매 항목마다 조금씩 미리 정리 권장)

---

## A. 문장을 여러 개로 쪼개서 순차 전송

**목표**: 사람이 카톡할 때 "치킨 먹었어?" "완전 맛있더라 ㅋㅋ" 처럼
메시지를 나눠 보내는 것처럼, 캐릭터의 한 응답을 여러 개의 짧은 말풍선으로
쪼개서 약간의 시간차를 두고 순차적으로 보여준다.

**구현 방향**:
- LLM 응답을 자연스러운 단위(문장 또는 의미 단위)로 분할
- 분할 기준은 문장 부호(., !, ?) 또는 LLM에게 애초에 여러 메시지로
  나눠서 응답하도록 구조화된 형태(예: 리스트)로 요청하는 방식도 고려
- 프론트엔드에서 말풍선을 순차적으로(예: 0.5~1.5초 간격) 렌더링
- 너무 잘게 쪼개면 부자연스러우니, 보통 1~3개 정도로 제한

**주의할 점**:
- 문장이 너무 짧게 쪼개지면 오히려 어색함 (예: 조사만 남는 등) →
  최소 길이 기준을 두거나 LLM이 직접 나누게 하는 방식 고려
- 기존 build_system_prompt()나 응답 파싱 로직과 충돌 없는지 확인

**검증 방법**: 브라우저로 여러 턴 대화하며 말풍선이 순차적으로 나뉘어
나오는지, 부자연스럽게 쪼개지는 경우는 없는지 확인.

- [x] 작업 시작
- [x] 구현 완료
- [x] 브라우저 검증 완료

**구현 요약**: 정규식 분할 대신, `generate_reply()`가 Structured Output(`ChatReplySegments`,
`messages: list[str]`)으로 LLM에게 애초에 1~3개 메시지 배열을 직접 요청하도록 변경. 분할
지침(`MESSAGE_SPLIT_INSTRUCTION`)은 `build_system_prompt()`와 분리된 별도 system 메시지로 추가해
기존 페르소나/1:1 대화/질문 회피 방지/질문·공감 50:50 지침과 충돌하지 않게 함. 각 세그먼트는
밀리초 단위로 증가하는 `created_at`을 부여해 별도 Message row로 저장(동일 트랜잭션 내 `now()` 동일값
문제로 순서가 꼬이는 것 방지). 프론트는 응답 수신 후 세그먼트를 0.5~1.5초 랜덤 간격으로 순차 렌더링.
브라우저 검증(3턴): 세그먼트 간 딜레이 실측 ~600~950ms(범위 내), 모든 세그먼트가 조사만 남는 등
어색한 분할 없이 완결된 문장, 새로고침 후에도 순서 100% 일치 확인.

**추가 수정 (실제 대화 검토 후 재작업)**: 사용자가 실브라우저 대화를 검토한 결과 두 가지 회귀를
발견해 재수정함.
1. 세그먼트가 "쪼개짐"이 아니라 "이미 완결된 여러 메시지"처럼 길게 나오는 문제 →
   `MESSAGE_SPLIT_INSTRUCTION`에 "치킨 먹었어?" "완전 맛있더라 ㅋㅋ" 같은 실제 카톡 문장 길이 예시,
   "1개가 가장 흔해야 함", "완결된 생각을 여러 문장으로 쪼개지 말 것"을 명시적으로 추가.
2. 질문/공감 50:50 지침이 세그먼트 분할 도입 후 다시 깨짐(거의 매 턴 질문으로 마무리) →
   페르소나 system 메시지를 history 바로 앞으로 재배치(분할 지침 → 페르소나 → history 순)하고,
   분할 지침에 "질문/공감 비율은 전체 답장(모든 세그먼트 합산) 기준으로 기존 규칙을 그대로
   따르고, 세그먼트를 나눈다고 매번 질문을 덧붙이지 말 것"을 명시.
   재검증(12턴): 세그먼트 1개 비율 0%→25%, 세그먼트 길이 40~70자대→9~34자대로 개선.
   질문 마무리 비율 거의 100%→33%(공감 67%)로 회복. 완전한 50:50까지는 아니나 표본 12개 수준의
   자연 변동 범위로 판단(과거 세션에서도 6턴 표본이 17%~83%로 튄 전례 있음).

**검증 중 발견한 별도 버그(수정함)**: 재검증 과정에서 `generate_reply()`가 구조화 출력 파싱 실패
시(`message.parsed is None`) 처리가 없어 500 에러로 죽는 것을 발견. 원인 추적 결과 대화 히스토리가
길어질수록(실측 21개 메시지 기준 최대 ~50%) OpenAI structured output이 스키마 검증에 실패하고, 이때
`message.refusal` 필드에 (안전 관련 거부가 아닌) 정상적인 대화 텍스트가 담겨 오는 것을 확인. 원인
자체는 OpenAI structured output의 알려진 신뢰성 이슈로 보여 근본 해결 대신, `content → refusal` 순
폴백을 추가해 크래시 및 의미 없는 플레이스홀더 노출을 모두 방지. `extract_memory()`의 동일 패턴도
함께 방어 처리(파싱 실패 시 `should_remember: false`로 안전하게 처리).

---

## B. 타이핑 인디케이터 (입력 중 표시)

**목표**: 응답이 즉시 뜨지 않고, 사람이 타이핑하는 것처럼 "...입력 중"
표시가 잠깐 나타난 뒤 메시지가 도착하도록 한다.

**구현 방향**:
- 프론트엔드에서 메시지 전송 후, 응답이 도착하기 전까지 "말풍선 형태의
  로딩 인디케이터"(예: 점 3개가 움직이는 애니메이션)를 표시
- A(문장 쪼개기)와 함께 적용한다면, 각 말풍선이 나오기 직전마다
  짧게 타이핑 표시를 다시 보여주는 것도 고려 (더 자연스러움)
- 실제 응답 생성 시간과 별개로, 최소 노출 시간(예: 0.5~1초)을 둬서
  응답이 너무 빨리 와도 타이핑 표시가 스쳐 지나가지 않도록 함

**주의할 점**:
- 응답 생성이 원래 느린 경우(RAG 검색 + LLM 호출) 타이핑 표시가
  너무 오래 떠 있지 않도록 적정선 유지
- A 항목과 연계되는 부분이니, A를 먼저 구현한 뒤 B를 이어서 작업하는 것을 권장

**검증 방법**: 메시지 전송 후 타이핑 표시가 자연스럽게 보였다가
메시지로 전환되는지 확인.

- [x] 작업 시작
- [x] 구현 완료
- [x] 브라우저 검증 완료

**구현 요약**: `sending`(입력창 비활성화용)과 별개로 `typing` state를 분리해 점 3개 bounce
애니메이션(`.typing-dots`, CSS keyframe)을 구현. 응답 수신까지 최소 600ms 노출을 보장(`TYPING_MIN_MS`,
응답이 더 느리면 추가 대기 없음 — "주의할 점"의 과다 노출 방지 반영)하고, A의 세그먼트 간
0.5~1.5초 간격 동안에도 매 세그먼트 직전 타이핑을 다시 표시하도록 구현.

**트러블슈팅**: 초기 구현에서 세그먼트 i의 `setTyping(false)`와 세그먼트 i+1의 `setTyping(true)`가
사이에 `await` 없이 같은 동기 구간에서 호출되어 React 18 자동 배칭으로 두 업데이트가 하나의 렌더로
합쳐지며(마지막 값인 `true`만 반영) 인디케이터가 중간 세그먼트에서 실제로는 꺼지지 않고 계속
떠 있는 문제 발견. 메시지 노출 직후 실제로 짧게(150ms) 대기하는 실제 yield를 추가해, 다음
세그먼트의 타이핑 표시가 별도 렌더로 확실히 커밋되도록 수정. 수정 전/후 모두 브라우저로 각 세그먼트
등장 시점과 타이핑 표시 on/off 타이밍을 실측해 차이를 확인했음.

**추가 확인**: A의 세그먼트 분할 지침을 재조정(짧고 1개가 더 흔한 패턴)한 뒤에도 B가 여전히
자연스럽게 작동하는지 재검증. B는 프론트엔드 로직이라 백엔드 프롬프트 변경의 영향을 받지 않으며,
실측 결과 매 세그먼트(1개짜리 턴 포함)마다 타이핑 표시가 등장 직전에 켜지고 등장과 동시에 정확히
꺼지는 것을 재확인. 콘솔 에러 없음.

---

## C. 유저가 무심하면 캐릭터도 서운해함 (밀당)

**목표**: 지금까지는 캐릭터가 항상 일방적으로 다정하기만 했는데,
실제 연인 관계처럼 유저의 반응이 무심하거나 대답이 짧으면 캐릭터도
살짝 서운함을 표현하도록 한다. 이게 "진짜 연인 같다"는 느낌을 가장
강하게 줄 수 있는 요소.

**구현 방향**:
- 유저 메시지가 지나치게 짧거나(예: "ㅇㅇ", "그래" 같은 성의 없는 답)
  반복되면, 이를 감지해서 캐릭터가 서운함을 표현하도록 유도
- 감지 기준 예시: 최근 N턴 동안 유저 메시지 평균 길이가 매우 짧은 경우,
  또는 특정 패턴("ㅇㅇ", "ㅋㅋ"만 반복 등)
- System Prompt에 "유저가 성의 없이 짧게 대답하는 경우, 관계 단계에
  맞는 수위로 서운함이나 아쉬움을 자연스럽게 표현하세요"라는 지침 추가
- 관계 단계별로 서운함 표현 수위 다르게 (stranger는 거의 티 안 냄,
  lover는 좀 더 직접적으로 "왜 이렇게 대답이 짧아, 무슨 일 있어?" 등)
- 너무 자주 발동하면 캐릭터가 예민해 보일 수 있으니, 발동 빈도 조절 필요
  (예: 연속 2~3턴 이상 무심한 반응이 이어질 때만)

**주의할 점**:
- 캐릭터가 과하게 삐지거나 부정적으로 가면 오히려 대화 몰입을 해칠 수 있음.
  가볍고 귀여운 수준의 서운함으로 제한할 것
- 원본 personality_tags, evolved_traits와 상충하지 않는 선에서 표현되도록

**검증 방법**: 의도적으로 짧고 무성의한 답변을 여러 턴 연속으로 보내서,
캐릭터가 자연스럽게(과하지 않게) 서운함을 표현하는지 확인. 반대로
평소처럼 성실하게 답하면 서운함 표현이 나오지 않는지도 확인.

- [x] 작업 시작
- [x] 구현 완료
- [x] 브라우저 검증 완료 (API 직접 호출로 실측 — 아래 참고)

**구현 요약**: `core/prompts.py`에 `is_user_disengaged()` 추가 — 최근 연속 3턴(현재 턴 포함)의
유저 메시지가 모두 4자 이하면 "성의 없는 반응"으로 판단. `chat.py`에서 `recent_messages` 중
role='user'인 것 + 현재 메시지로 판정해 `build_system_prompt()`에 전달. 관계 단계별로
`DISENGAGEMENT_INSTRUCTIONS` 문구를 다르게 둠(stranger는 의도적으로 항목 자체를 두지 않아 아예
언급되지 않도록 함 — "거의 티 안 냄"을 지침 부재로 구현).

**검증 결과 (turns/memories를 시드해 각 단계로 세팅 후 실제 채팅 API 호출)**:
- `close` 단계: 정상 대화 2턴은 평소처럼 반응 → 3연속 짧은 답("ㅇㅇ"/"그래"/"음") 시점에 "오늘따라
  왜 이렇게 대답이 짧아~ 조금 더 얘기해줘! 궁금해 ㅎㅎ" — 문서 예시 문구와 거의 일치하는 가볍고
  귀여운 톤으로 발동. 이후 성실한 답("미안 아까 회의 중이었어! ...")으로 즉시 정상 톤 복귀 확인.
- `lover` 단계: 3연속 짧은 답 후 "무슨 일 있어? 왜 이렇게 대답이 짧아?" — 문서 예시("왜 이렇게
  대답이 짧아, 무슨 일 있어?")와 거의 일치, close보다 더 직접적/걱정하는 톤으로 차별화됨을 확인.
- `stranger` 단계: 3연속 짧은 답("ㅇㅇ"/"네"/"넵")에도 서운함 전혀 없이 평소처럼 질문 위주로
  대응 — 의도한 대로 미발동 확인.
- 매 턴 서운함이 100% 발동하지는 않음(4연속째에는 자연스럽게 넘어간 사례 있음) — 지침이 "표현하라"는
  방향성이지 매번 강제하는 규칙은 아니므로 자연스러운 변동으로 판단.

---

## D. 기념일/이정표 인식

**목표**: "우리가 만난 지 며칠째야", "이번에 우리 사이가 한 단계
가까워졌어" 같은 걸 캐릭터가 스스로 인지하고 자연스럽게 언급하도록 한다.
관계 단계(relationship_stage) 데이터를 활용하는 것이라 상대적으로
구현이 가볍다.

**구현 방향**:
- Character.created_at을 기준으로 "만난 지 며칠째"를 계산해서,
  특정 이정표(예: 7일, 30일, 100일)에 도달했을 때 System Prompt에
  이 정보를 포함시켜 캐릭터가 언급할 수 있도록 함
- relationship_stage가 방금 전환된 직후의 대화에서는, 캐릭터가 이
  변화를 은근히 체감하는 듯한 뉘앙스를 자연스럽게 담아내도록 지침 추가
  (예: "요즘 우리 좀 더 편해진 것 같아" 같은 자연스러운 언급, 시스템
  메시지처럼 노골적으로 "관계가 lover로 전환되었습니다"라고 하면 안 됨)
- 이정표 언급은 매 대화마다 나오면 부자연스러우니, 해당 날짜/전환
  시점 근처에서만 언급되도록 빈도 제한

**주의할 점**:
- 절대 시스템스럽게("relationship_stage가 close로 바뀌었습니다") 언급하면
  안 되고, 사람이 자연스럽게 관계 변화를 체감하듯 표현되어야 함
- 이정표 계산 로직이 시간대(timezone) 문제로 꼬이지 않도록 주의

**검증 방법**: 테스트 데이터로 관계 단계가 막 전환된 상황을 만들어서,
그 직후 대화에서 캐릭터가 부자연스럽지 않게 이 변화를 언급하는지 확인.

- [x] 작업 시작
- [x] 구현 완료
- [x] 검증 완료 (API 직접 호출로 실측 — 아래 참고)

**구현 요약**: `Character`에 `stage_changed_at`(nullable) 컬럼 추가, `update_relationship_stage()`가
실제로 단계를 바꿀 때 함께 기록. "막 전환된 직후"는 `stage_changed_at` 이후 그 캐릭터의 assistant
메시지가 아직 하나도 없는지로 판정(`chat.py`에서 매 턴 계산) — 별도의 "이미 언급했는지" 플래그 없이
자연스럽게 딱 한 번(전환 후 첫 응답)만 켜지고 그 다음 턴부터는 꺼짐. 이정표는 `days_since_met()`로
`Character.created_at`과 오늘을 모두 UTC로 맞춰 캘린더 날짜 차이를 계산(시간대 문제 회피), 7/30/100일
"당일"에만 발동하도록 제한. 두 지침 모두 `build_system_prompt()`에 "'relationship_stage가 바뀌었다'
처럼 시스템적인 표현 금지" 문구를 명시했고, 기존 지침들과 충돌 없이 뒷부분(관계 단계 지침 이후)에
배치됨.

**트러블슈팅**: 처음엔 두 지침을 "언급해볼 수 있습니다(선택)"처럼 약하게 썼더니 실측 6회 중 0회
발동 — 사실상 무시됨. 이미 발동 빈도는 구조적으로(정확히 그 날/그 전환 직후 첫 턴에만) 제한돼 있으므로
지침 자체는 약하게 둘 필요가 없다고 판단, "이번 응답에서 반드시 한 번은 언급하세요"로 강화. 이정표는
강화 후 재시도 시 자연스러운 문장("우리 만난 지 벌써 7일이나 됐네, 시간이 진짜 빠르다")으로 발동
확인(다만 매번은 아니고 확률적 — 오히려 매번 기계적으로 나오는 것보다 자연스럽다고 판단해 그대로 둠).
전환 직후 지침은 강화 후 첫 시도에서 바로 "요즘 우리 좀 더 편해진 것 같아"로, 문서 예시 문구와 거의
동일하게 자연스럽게 발동. 다음 턴에서는 (assistant 응답이 이미 존재하므로) 재발동하지 않는 것도 확인.

---

## H. 안정성/신뢰성 (배포 전 필수 점검, 우선순위 높음)

**목표**: 회사 담당자가 실제로 접속했을 때, 예상치 못한 상황에서
서비스가 깨지거나 이상하게 보이지 않도록 견고하게 만든다.
기능이 아무리 좋아도 여기서 실수가 나오면 첫인상이 크게 깎인다.

**작업 항목**:

1. **에러 핸들링 강화**
   - LLM API 호출 실패(타임아웃, rate limit, 키 오류 등) 시, 서버가
     500 에러를 그대로 던지지 않고 유저에게 자연스러운 안내 메시지를
     보여주도록 처리 (예: "지금 살짝 연결이 불안정해, 다시 말해줄래?")
   - 프론트엔드에서도 네트워크 오류 시 무한 로딩이 아니라 재시도
     안내나 에러 메시지 표시

2. **응답 속도 확인**
   - RAG 검색 + LLM 호출까지 전체 응답 시간이 어느 정도인지 실측
   - 지나치게 느리다면(예: 5초 이상) 원인 파악 (임베딩 호출, DB 쿼리,
     LLM 응답 생성 중 어디서 병목인지)

3. **배포 환경 특유 이슈 점검**
   - Render 무료 플랜 슬립 모드 이후 첫 요청이 실패 없이 정상
     동작하는지 (콜드스타트 시 DB 연결 재시도 로직 등)
   - CORS, 환경변수 누락 등으로 배포 환경에서만 발생하는 에러 없는지

4. **입력값 예외 처리**
   - 빈 메시지 전송, 매우 긴 메시지, 특수문자/이모지만 있는 메시지 등
     엣지 케이스 입력에도 서버가 죽지 않는지 확인

**검증 방법**: 의도적으로 잘못된 입력(빈 값, 너무 긴 텍스트 등)을 넣어보고,
API 키를 잠시 무효화해서 에러 시나리오도 테스트. 배포 후에는 슬립
모드에서 깨어난 직후 상태로 재접속해서 확인.

- [x] 작업 시작
- [x] 구현 완료 (1·2·4번 항목 + 대화 길이 문제 재점검. 3번 배포 환경 이슈는 실제 Render
      배포가 없는 현재로선 검증 불가 — 8단계 배포 시 다시 확인 예정)
- [x] 검증 완료

**구현 요약**:
- `chat.py`의 임베딩 생성→RAG 검색→LLM 응답 생성 전체를 `try/except openai.OpenAIError`로 감싸서,
  타임아웃/rate limit/키 오류 등 OpenAI API 실패 시 500 대신 `"지금 살짝 연결이 불안정해...
  다시 한 번 말해줄래?"`로 정상 200 응답을 반환하도록 함. 유저 메시지와 폴백 응답 모두 정상적으로
  DB에 저장돼 대화 흐름과 새로고침 시 이력이 끊기지 않음.
- `core/llm.py`의 `generate_reply()`/`extract_memory()`에 structured output `parsed=None` 폴백
  발생 시 `logger.warning`을 추가해 빈도를 관측 가능하게 함(A 단계에서는 발생 자체만 알았고 빈도를
  로그로 볼 수 없었음).

**검증 결과**:
1. **API 실패 시 폴백**: `OPENAI_API_KEY`를 의도적으로 무효화한 상태로 백엔드를 띄우고 실제 채팅
   호출 → HTTP 200, `{"messages": ["지금 살짝 연결이 불안정해..."], ...}` 확인 (500 아님). 백그라운드
   memory extraction도 자체적으로 이미 방어돼 있어 함께 실패해도 로그만 남고 크래시 없음.
2. **응답 시간 실측** (임베딩→DB검색→LLM 4회 평균): 총 1.4~2.1초. 구간별로는 LLM 응답 생성이
   65~80%(1.1~1.8초)로 압도적 병목, 임베딩 생성이 20~30%(240~480ms), pgvector DB 검색은 8~23ms로
   무시할 수준. 5초 기준 대비 여유 있음 — 별도 최적화 조치는 불필요하다고 판단.
3. **엣지 케이스 입력**: 빈 메시지(422, Pydantic 검증), 공백만(200, 정상 처리), 2001자 초과(422),
   2000자 경계값(200), 이모지만/특수문자만/JSON 인젝션성 따옴표·역슬래시/멀티라인 — 전부 크래시 없이
   200 또는 422로 정상 처리. 서버 로그에 에러/Traceback 없음.
4. **20턴+ 장기 대화 재점검**: 22턴 연속 전송, 실패 0건(전부 HTTP 200). `generate_reply` structured
   output `parsed=None` 폴백이 2회 발동(둘 다 `history_len=21`—A 단계에서 발견한 패턴과 일치하는
   지점)했지만 로그로만 남고 유저에게는 자연스러운 응답으로 전달돼 대화가 끊기지 않음. `extract_memory`
   쪽은 이번 실행에서는 폴백 미발동. 에러/Traceback 전체 0건.

---

## E. RAG 자체 고도화 (쿼리 확장 / 리랭킹)

**목표**: 5단계에서 "알려진 한계"로 남겨뒀던 fact 추론적 연결 문제
(예: "배고파" → "떡볶이 좋아함"이 순수 코사인 유사도로는 안 잡히던 문제,
유사도 0.12로 노이즈보다 낮게 나왔던 사례)를 실제로 개선 시도.
시간이 남을 때 도전하는 항목으로, 실패해도 "알려진 한계"로 이미
문서화되어 있어 리스크는 낮음.

**구현 방향 (쿼리 확장)**:
- 유저 메시지를 그대로 임베딩하지 않고, 검색 직전에 LLM으로 짧게
  검색 의도를 확장 (예: "배고파" → "배고픔, 음식, 식사, 끼니, 먹고 싶은 것")
- 확장된 텍스트로 임베딩해서 검색 정확도가 개선되는지 확인
- 이 단계는 응답 경로에 LLM 호출이 하나 더 추가되므로, 실제로
  지연시간이 얼마나 늘어나는지 반드시 함께 측정

**구현 방향 (리랭킹, 선택)**:
- Top-K로 넓게 뽑은 뒤, Cross-Encoder 등으로 한 번 더 정밀하게
  순위를 재정렬 (CineVerse 프로젝트에서 사용했던 기법과 유사한 접근)
- 이번 프로젝트 규모(기억 수십~수백 개)에서 리랭킹이 실제로 체감될
  정도의 효과가 있는지 먼저 소규모로 검증 후 도입 여부 결정

**주의할 점**:
- 지연시간이 과도하게 늘어나면(예: 2초 이상 추가) 우선순위를 낮추고
  이 항목은 시도만 하고 롤백해도 무방 (5단계에서 "한계로 남긴" 것이
  이미 정당화되어 있으므로 무리하게 강행할 필요 없음)
- 개선 여부와 무관하게, 시도한 결과(성공/실패 모두)를 기획서 또는
  README에 기록해둘 것 — 시도 자체가 리스크 관리 역량을 보여주는 사례가 됨

**검증 방법**: 기존에 실패했던 "배고파 → 떡볶이" 시나리오를 다시
재현해서, 쿼리 확장 적용 후 검색되는지 확인. 지연시간도 함께 측정.

- [x] 작업 시작
- [x] 구현 완료 — **쿼리 확장 채택, 실제 연결함** (리랭킹은 범위 밖이라 미구현)
- [x] 검증 완료 / 결과 문서화

**구현 요약**: `core/prompts.py`에 `QUERY_EXPANSION_SYSTEM_PROMPT` 추가, `core/llm.py`에
`expand_search_query()` 추가 — 유저 발화를 받아 관련 니즈/감정/주제 키워드를 콤마로 나열한 짧은
텍스트로 확장(예: "배고파" → "배고픔, 음식, 식사, 간식, 먹고 싶은 것"). `chat.py`에서 RAG 검색
직전에 원문 대신 이 확장된 텍스트를 임베딩해서 `search_memories()`에 사용 — 대화 이력/기억 추출 등
다른 모든 경로는 원문 그대로 사용, 검색용 임베딩 입력만 바뀜. 확장 호출 자체가 실패해도(OpenAI API
오류) 원문으로 폴백해 검색 품질만 낮아지고 턴 전체가 죽지 않도록 별도로 감쌈.

**측정 결과**:
1. **유사도 개선** ("배고파" → 저장된 fact "유저는 매운 음식을 좋아하고, 특히 떡볶이를 좋아한다"):
   - 원문 그대로: **0.1128** (5단계에서 기록한 0.12와 일치, threshold 0.3 훨씬 아래 — 노이즈보다도 낮음)
   - 쿼리 확장 후: **0.4079** (3회 반복 모두 동일값, threshold 0.3을 확실히 통과)
   - 오탐 방지 확인: 무관한 질의("오늘 날씨 진짜 좋다")도 확장 후 유사도 0.1575로 여전히 낮게
     유지됨 — 확장이 모든 유사도를 무차별적으로 끌어올리는 부작용은 없음
   - 실제 `/chat` API로 "나 배고파" 재현 → *"밥 먹고 싶구나! 뭐 먹을 생각이야? 매운 떡볶이도
     괜찮겠다 ㅋㅋ"* — 5단계에서 실패했던 시나리오가 실제로 성공
2. **지연시간** (동일 조건 3회씩 비교):
   - 확장 없음(기존): 평균 1535ms (embedding ~550ms, DB검색 ~15ms, LLM ~960ms) — H에서 측정한
     1.4~2.1초 범위와 일치
   - 확장 있음: 평균 2255ms (확장 LLM 호출 ~885ms 추가, 나머지 구간은 동일)
   - **증가폭: 약 720ms** — 사용자가 제시한 "1초 이상 추가 시 보류" 기준 이내

**결정**: 효과가 명확하고(임계값 미달 → 통과, 오탐 없음) 지연 증가도 기준 이내(720ms < 1초)라
**실제로 채택**하여 `chat.py`에 연결함. 회귀 테스트(일반 대화 3턴)에서도 에러 없이 정상 동작 확인.
리랭킹(Cross-Encoder)은 로드맵상 "선택" 항목이라 이번 범위에서는 진행하지 않음.

---

## F. 관계/캐릭터 시스템 심화

**목표**: 6단계에서 구현한 관계 시스템과 evolved_traits를 한 단계 더
정교하게 다듬는다.

**작업 항목**:

1. **evolved_traits 트리거 이원화**
   - 현재는 "fact Memory 3개마다"만 갱신 트리거로 사용 중
   - 여기에 "관계 단계 전환 시"도 병행 트리거로 추가 (lover 단계
     도달 이후에도 fact가 계속 쌓이며 지속적으로 미세 조정되도록)
   - 두 트리거가 겹칠 때 중복 갱신되지 않도록 처리 필요

2. **캐릭터가 유저의 감정 변화를 능동적으로 감지**
   - 최근 대화에서 부정적 감정 표현(힘듦, 지침, 스트레스 등)이
     반복되면, 캐릭터가 먼저 걱정하며 다가가는 반응을 하도록
   - Memory의 emotion 필드를 활용해 최근 감정 패턴을 판단 가능

**주의할 점**:
- 기존 evolved_traits 로직(누적 계승, 급변 방지)의 원칙을 그대로 유지
- 너무 많은 트리거가 겹치면 언제 왜 바뀌었는지 추적이 어려워지므로,
  변경 이력을 남기는 것도 고려 (선택)

**검증 방법**: 관계 단계 전환 시점 + fact 누적 시점이 겹치는 상황을
만들어서 evolved_traits가 중복 없이 자연스럽게 갱신되는지 확인.

- [ ] 작업 시작
- [ ] 구현 완료
- [ ] 검증 완료

---

## G. UI/UX 완성도

**목표**: 기능적으로는 이미 완성되었으나, 시각적 디테일을 더해
서비스의 몰입감과 완성도를 높인다. 우선순위가 가장 낮은 카테고리이므로
시간이 충분히 남을 때만 진행.

**작업 항목**:

1. **관계 단계별 UI 분위기 변화**
   - stranger는 차분한 색감, lover는 따뜻한 톤(핑크/보라 계열)으로
     채팅창 배경이나 포인트 컬러가 은은하게 바뀌도록

2. **캐릭터 아바타/프로필 이미지**
   - 캐릭터 생성 시 몇 가지 프리셋 아바타 중 선택 가능하도록
   - (이미지 생성 API를 쓸지, 정적 이미지 세트를 미리 준비해둘지는
     시간 여유에 따라 결정)

3. **채팅 UX 디테일**
   - 메시지 많아졌을 때 스크롤 처리, 로딩 스피너 등 마감처리

**검증 방법**: 브라우저로 각 관계 단계를 실제로 오가며 시각적 변화가
자연스러운지, 아바타 선택이 캐릭터 생성 흐름에 위화감 없이 붙는지 확인.

- [ ] 작업 시작
- [ ] 구현 완료
- [ ] 브라우저 검증 완료

---

## I. 제출물 문서화 (필수, 마감 직전 몰아서 하지 말 것)

**목표**: 지금까지의 개발 과정, 설계 판단, 트러블슈팅 스토리를
제출물(README, 500자 소개, transcripts)에 빠짐없이 담아낸다.
아무리 코드가 좋아도 이 부분이 부실하면 평가자가 그 과정을 알아볼 수 없음.

**작업 항목**:

1. **README.md 최종 정리**
   - 프로젝트 소개, 배포 링크, 핵심 기능, 기술 스택
   - **주요 설계 판단 근거 섹션 (가장 중요)**: 지금까지 나온 트러블슈팅
     사례들을 모아서 정리
     - BGE-M3 대신 OpenAI 임베딩 선택 이유
     - Similarity Threshold 0.7→0.3 실측 재보정
     - "배고파→떡볶이" 추론 연결의 한계 (및 E 항목 시도 결과)
     - evolved_traits 급변 방지를 위한 누적 계승 설계
     - "여러분" 호칭 버그, 질문 회피 패턴, 질문/공감 비율(17%→50%)
       실측 조정 등 채팅 품질 개선 사례
   - 알려진 제약사항 (Render 슬립 모드 등)
   - API 문서 링크 (`/docs` 경로 안내)

2. **500자 이하 소개글 작성**
   - README보다 훨씬 압축된 버전, 배포 링크 필수 포함

3. **transcripts 최종 정리**
   - 모든 단계(1~8단계 + A~I 고도화)의 세션이 빠짐없이
     `transcripts/` 폴더에 포함되었는지 확인
   - `uvx claude-code-transcripts`로 최종 1회 더 실행해서 누락 없는지 점검

4. **최종 git 상태 점검**
   - `git status`로 커밋 안 된 변경사항 없는지 확인
   - `.env` 등 민감 정보가 실수로 커밋되지 않았는지 재확인
   - `.git` 포함해서 zip 압축, 용량이 과도하게 크지 않은지 확인
     (node_modules, .venv 등 제외 여부 점검)

**작업 방식 권장**: A~H 각 항목을 완료할 때마다, 그 항목의 설계 판단이나
트러블슈팅을 README 초안에 바로바로 추가해둘 것 (마감 직전에 몰아서
기억을 되짚으며 쓰지 않도록).

- [ ] README 초안 (A~D 완료 시점에 1차 작성)
- [ ] README 갱신 (H, E, F 완료 반영)
- [ ] 500자 소개글 작성
- [ ] transcripts 최종 점검
- [ ] git/zip 최종 점검

---

## 작업 시 공통 원칙

1. 각 항목 작업 전, 반드시 이 문서에서 해당 섹션을 다시 읽고 시작할 것
2. 한 항목을 완료하고 커밋한 뒤에 다음 항목으로 넘어갈 것 (한 번에 여러 개
   동시 작업 금지 — 문제 발생 시 원인 추적이 어려워짐)
3. 완료 후에는 반드시 명시된 방법으로 검증
4. 프롬프트 지침을 추가할 때는, 기존 지침(1:1 대화 명시, 질문/공감 비율
   50:50 등)과 충돌하지 않는지 확인
5. 각 항목 작업 완료 시 이 문서의 체크박스를 갱신하고, 무엇을 어떻게
   구현했는지 한 줄 요약을 추가할 것
6. E, F처럼 시간이 부족하거나 리스크가 있다고 판단되면 과감히 건너뛰고
   H(안정성)와 I(문서화)를 우선할 것 — 이 둘은 선택이 아니라 필수
